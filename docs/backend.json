
{
  "entities": {
    "CampusItem": {
      "title": "Campus Item",
      "description": "A lost or found item reported on campus",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "Brief title of the item"
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the item"
        },
        "category": {
          "type": "string",
          "description": "Category of the item (electronics, apparel, etc.)"
        },
        "type": {
          "type": "string",
          "enum": ["lost", "found"],
          "description": "Whether the item is lost or found"
        },
        "location": {
          "type": "string",
          "description": "Where the item was lost or found"
        },
        "imageUrl": {
          "type": "string",
          "description": "Base64 encoded image data or image URL"
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who created the report"
        },
        "status": {
          "type": "string",
          "enum": ["open", "matched", "closed"],
          "description": "Lifecycle status of the report"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Firestore server timestamp"
        }
      },
      "required": [
        "title",
        "description",
        "category",
        "type",
        "location",
        "userId",
        "status",
        "createdAt"
      ]
    },
    "User": {
      "title": "User Profile",
      "description": "Registered user profile details",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string" },
        "phone": { "type": "string" },
        "profileImage": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Match": {
      "title": "Item Match",
      "description": "A confirmed match between a lost and found item",
      "type": "object",
      "properties": {
        "lostItemId": { "type": "string", "description": "Reference ID to items collection" },
        "foundItemId": { "type": "string", "description": "Reference ID to items collection" },
        "matchedAt": { "type": "string", "format": "date-time" },
        "status": {
          "type": "string",
          "enum": ["pending", "approved", "rejected"]
        }
      }
    }
  },
  "auth": {
    "providers": [
      "google.com",
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/items/{itemId}": {
      "schema": "CampusItem",
      "description": "Collection of all lost and found item reports"
    },
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile collection"
    },
    "/matches/{matchId}": {
      "schema": "Match",
      "description": "Collection of identified item matches"
    }
  }
}
