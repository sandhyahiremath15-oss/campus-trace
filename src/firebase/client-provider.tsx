'use client';

/**
 * @fileOverview This file is now deprecated and its functionality has been merged into provider.tsx
 * to avoid circular dependencies and multiple initialization exceptions.
 */
export { FirebaseClientProvider } from './provider';
